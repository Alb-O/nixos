# Placeholder for hardware-configuration.nix
# Generate this file on your desktop using nixos-generate-config
{ config, lib, pkgs, modulesPath, ... }:

{
  # Add hardware-specific configuration here after running nixos-generate-config
}
